name: feature_player
description: Touchline player features — squad hub, player profiles, and lineup displays.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Internal
  api_client:
    path: ../api_client
  core_ui:
    path: ../core_ui
  theming:
    path: ../theming

  # State
  flutter_riverpod: ^2.6.1

  # UI
  cached_network_image: 3.4.1
  smooth_page_indicator: 1.1.0
  font_awesome_flutter: 10.7.0
  flutter_spinkit: 5.2.0

  # Utils
  collection: 1.19.1
  url_launcher: ^6.3.0

dev_dependencies:
  flutter_lints: 4.0.0
  flutter_test:
    sdk: flutter
