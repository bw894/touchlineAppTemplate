name: api_client
description: Touchline REST API client, endpoint definitions, and all data models. Pure Dart — no Flutter dependency.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  collection: 1.19.1
  equatable: 2.0.7
  http: 1.4.0
  json_path: 0.7.2
  uuid: 4.5.1

dev_dependencies:
  lints: 4.0.0
  test: ^1.24.0
