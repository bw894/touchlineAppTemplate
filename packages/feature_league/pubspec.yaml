name: feature_league
description: Touchline league tables — full table, compact, and mini variants.
publish_to: 'none'
version: 0.0.1

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  # Internal
  api_client:
    path: ../api_client
  core_ui:
    path: ../core_ui
  theming:
    path: ../theming

  # State
  flutter_riverpod: ^2.6.1

  # Utils
  collection: 1.19.1

dev_dependencies:
  flutter_lints: 4.0.0
  flutter_test:
    sdk: flutter
